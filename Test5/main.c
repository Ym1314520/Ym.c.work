#include<stdio.h>
int main()
{
    int sum,x,a,b,c,d;
    for(a=1;a<=9;a++£©
    for(b=0;b<=9;b++)
    for(c=0;c<=9;c++)
    for(d=0;d<=i;d++)
    if(a==b&&c==d&&b!=c)
    {
    sum=a*1000+b*100+c*10+d;
    for(x=1;;x++)
    if(sum/x*x==1)
    }
    printf("The number is:%d\n",sum);
    return 0;

}

